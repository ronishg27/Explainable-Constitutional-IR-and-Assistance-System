$kind: http-request
version: v3
name: Ask
url: "{{base_url}}/ask"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "query": " Discuss the concept of sovereignty and state power in Nepal."
    }
order: 3000
